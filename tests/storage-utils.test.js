// Test storage fallback behavior
describe('StorageUtils', () => {
    it('should fallback to sessionStorage when localStorage fails', () => {
        // Mock localStorage to throw
        // Test sessionStorage is used
    });

    it('should use in-memory storage when both fail', () => {
        // Mock both storages to fail
        // Verify in-memory storage works
    });
});