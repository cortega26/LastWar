{
  "name": "lastwar-tools",
  "version": "1.2.0",
  "description": "Strategic Command Center for Last War: Survival",
  "scripts": {
    "dev": "python -m http.server 8080",
    "build": "echo 'Static site - no build needed'",
    "test": "npm run test:lighthouse",
    "test:lighthouse": "lhci autorun --config=lighthouserc-enhanced.js",
    "test:performance": "node scripts/performance-budget-check.js",
    "analyze": "npm run test:lighthouse && npm run test:performance"
  },
  "devDependencies": {
    "@lhci/cli": "^0.12.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}